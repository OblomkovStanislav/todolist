import {Paper, styled} from "@mui/material";

export const TodolistPaperStyled = styled(Paper)({
    color: "#fff",
    background: "#1c1c1c",
    borderRadius: "10px",
    boxShadow: "0px 0px 5px 5px rgba(0, 0, 0, 0.5)",
});
